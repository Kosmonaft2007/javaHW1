package ru.homeWort21;

public class Main {
    public static void main(String[] args) {

        TreeNode(isSameTree(1, 6);
        System.out.println(isSameTree(path));

    }

    public static boolean isSameTree(TreeNode p, TreeNode q) {
        char[] firstRoot = {1, 2, 3,};
        char[] secondRoot = {1, 2, 3,};
        if (p.length == q.length)
            return true;
        if (p.length != q.length)
            return false;
        return isSameTree(path);
    }
}
